#pragma once

#include <SharedBase/types/ge_def.h>

template <typename CLASS>
class bTPOSmartPtr
{
    GE_UNIMPL_STRUCT(bTPOSmartPtr)

  private:
    GE_UNIMPL_MEMBERS(8)
};

GE_ASSERT_SIZEOF(bTPOSmartPtr<void>, 8)
